version: '3.5'
services:
  ds_db:
    build:
      context: .
      dockerfile: Dockerfile
    image: postgres
    restart: always
    volumes:
      - ds_pgdata:/var/lib/postgresql/data
      - ~/Coding/repos/bitbucket/competitions/projHomeCredit/input:/home/jovyan/ecam_ds/data/input
      - ~/Coding/repos/github/konyshev/ecam_ds:/home/jovyan/ecam_ds
    ports: 
      - "5432:5432"
volumes:
  ds_pgdata:
